<p id="notice"><%= notice %></p>
<div class="row">
    <div class="col-md-6">
        <div class="thumbnail">
            <%= image_tag @my_listing.image.url%>
        </div>
    </div>
    <div class="col-md-6">
        <h3> <%= @my_listing.name %></h3>
        <p><%= number_to_currency(@my_listing.price) %></p>
        <p><%= @my_listing.description %></p>
    </div>
    </div>

<%= link_to 'Edit', edit_my_listing_path(@my_listing), class:"btn btn-link"  %> |
<%= link_to 'Back', my_listings_path, class:"btn btn-link"  %>
